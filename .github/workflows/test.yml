name: DP Run Tests
on:
  push:
  pull_request:
jobs:
  test:
    runs-on:  ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: sbt/setup-sbt@v1
      - name: Run tests
        run: sbt scalafmtCheckAll test
